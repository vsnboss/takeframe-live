const menuButton=document.querySelector('.menu-button');const nav=document.querySelector('.site-nav');if(menuButton&&nav){menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}))}
const dialog=document.querySelector('#demo-modal');document.querySelectorAll('[data-open-demo]').forEach(button=>button.addEventListener('click',()=>dialog?.showModal()));dialog?.addEventListener('close',()=>{if(dialog.returnValue!=='default')return;const form=dialog.querySelector('form');const data=new FormData(form);const subject=encodeURIComponent('TAKEFRAME demo request');const body=encodeURIComponent(`Name: ${data.get('name')}\nCompany: ${data.get('company')}\nEmail: ${data.get('email')}\nProduction type: ${data.get('type')}\n\n${data.get('message')||''}`);window.location.href=`mailto:office@vsn.hr?subject=${subject}&body=${body}`});
const revealObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('in');revealObserver.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>revealObserver.observe(el));
const track=document.querySelector('.graphics-track');document.querySelector('.carousel-arrow.next')?.addEventListener('click',()=>track?.scrollBy({left:track.clientWidth*.8,behavior:'smooth'}));document.querySelector('.carousel-arrow.prev')?.addEventListener('click',()=>track?.scrollBy({left:-track.clientWidth*.8,behavior:'smooth'}));
document.querySelectorAll('.console-tabs button').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.console-tabs button').forEach(item=>item.classList.remove('active'));button.classList.add('active')}));
document.querySelectorAll('.preview').forEach(preview=>preview.addEventListener('click',()=>{document.querySelectorAll('.preview').forEach(item=>item.classList.remove('active'));preview.classList.add('active')}));